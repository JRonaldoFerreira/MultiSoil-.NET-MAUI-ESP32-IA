<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="MultiSoil_EdgeAI.Views.TalhoesPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Talhões">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Adicionar" Clicked="OnAddClicked" Priority="0"/>
    </ContentPage.ToolbarItems>

    <VerticalStackLayout Padding="16" Spacing="12">
        <CollectionView x:Name="ListView" SelectionMode="Single" SelectionChanged="OnSelectionChanged">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems Mode="Execute">
                                <SwipeItem Text="Editar" Invoked="OnEditSwipe" BackgroundColor="{DynamicResource SecondaryColor}"/>
                                <SwipeItem Text="Excluir" Invoked="OnDeleteSwipe" BackgroundColor="{DynamicResource ErrorColor}"/>
                            </SwipeItems>
                        </SwipeView.RightItems>
                        <Grid Padding="12" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                            <Label Grid.Row="0" Text="{Binding Nome}" FontAttributes="Bold" />
                            <Label Grid.Row="1" Text="{Binding Cultura}" FontSize="12" Opacity="0.7"/>
                            <Label Grid.Column="1" Grid.RowSpan="2" Text="{Binding Microcontrolador}" VerticalOptions="Center" FontSize="12"/>
                        </Grid>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
